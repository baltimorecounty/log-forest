'use strict';

describe('logForest.logs module', function() {

  beforeEach(module('logForest.logs'));

  describe('logs controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var logsCtrl = $controller('LogsCtrl');
      expect(logsCtrl).toBeDefined();
    }));

  });
});